apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-ocr-worker.fullname" . }}-test-connection"
  labels:
{{ include "open-ocr-worker.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "open-ocr-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
